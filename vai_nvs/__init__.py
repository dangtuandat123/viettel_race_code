"""VAI NVS — BTS Digital Twin Novel View Synthesis pipeline (VAR 2026 round 1).

Torch/gsplat imports are kept lazy so that audit/prepare/submission tooling
runs on CPU-only machines (e.g. the local Windows box holding the data).
"""

__version__ = "1.0.0"
